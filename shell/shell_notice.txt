使用的每个文件，每个字符串都应该做异常处理 -e(文件是否存在) -z（字符串是否为空）
shell  除了赋值操作，其他的第一个标识符一定要是命令
工作目录是当前所在目录，不是脚本所在目录,脚本中应当包含把工作目录切换到当前目录的操作readlink,脚本移了一个路径，也要让他能够继续使用
脚本路径很重要，尽量不要写死，尽量用灵活的
PWD（全部大写）就相当于取得当前路径的命令
“”弱引用，部分视为转义
''强引用，全部做字符处理
··（反引号），无引用，全部看做转义


异常处理


路径要支持可扩展
路径最好以一个为基准，使用相对路径
